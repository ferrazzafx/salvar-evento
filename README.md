# Salvar evento

Página usada pela extensão **Evento no Zap** para salvar eventos no calendário do celular.

Os dados de cada evento vão no próprio link, depois do `#`. Eles não são enviados a este servidor e nada é armazenado aqui.
